name: CI/CD Deploy

on:
  push:
    branches: [main]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: self-hosted
    env:
      WEB_PORT: 8080
    steps:
      - uses: actions/checkout@v4

      - name: Build images
        run: docker compose build --no-cache

      - name: Start stack
        run: docker compose up -d --wait

      - name: Wait for services to be healthy
        run: docker compose ps

      - name: Test endpoints
        run: |
          curl.exe -f http://localhost:${{ env.WEB_PORT }}/api/health
          curl.exe -f http://localhost:${{ env.WEB_PORT }}/api/items
          curl.exe -f http://localhost:${{ env.WEB_PORT }}/

      - name: Print logs on failure
        if: failure()
        run: docker compose logs

      - name: Cleanup
        if: always()
        run: docker compose down -v
